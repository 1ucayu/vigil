"""Stage 4: DSL Semantic Guard Generation.

Annotates FSM transitions with guard conditions using a constrained formal grammar
(docs/dsl_grammar.lark). Uses LLM + constrained decoding to generate syntactically
correct guards.
"""
