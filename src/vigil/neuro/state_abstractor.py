"""Stage 2: State Abstraction.

Currently uses rule-based structural fingerprinting (in RawScreen.get_structural_fingerprint).
Future: LLM-assisted abstraction for ambiguous cases.
"""

from __future__ import annotations


class StateAbstractor:
    """Placeholder for future LLM-assisted state abstraction.

    Currently, state abstraction is handled entirely by structural fingerprinting
    in RawScreen.get_structural_fingerprint() and FsmBuilder._build_states().
    """

    pass
